In order to connect the bot to Discord, the bot's token must be stored in `token.txt` at the top level of the project directory.

The server and channel IDs in `main.py` currently point to a test server and must be changed locally to IDs that correspond to a server you have access to.
